---
title: Развязки
aliases:
  - Endings
  - Финальная битва
tags:
  - ваншот
  - финал
  - интерактив
  - rpg
authors:
  - Gleuderoff
date: 2025-07-31
cssclasses:
  - finale-ritual
  - interactive-story
light_cnt: "0"
moral_cnt: "0"
tragic_cnt: "0"
chaos_cnt: "0"

---

# 🔚 Развязки

> _«Когда тишина опустится на руины, судьбы будут запечатаны навеки...»_
> 
> *Древние силы пробуждаются. Выбор героев определит судьбу мира.*

Тысячи путей привели героев сюда — к древней часовне, где переплетаются нити судьбы. **Свет** и **Тьма**, **Порядок** и **Хаос**, **Мудрость** и **Безумие** — что возьмёт верх в финальной битве?

---

```dataviewjs
const { update } = app.plugins.plugins["metaedit"].api;
const filePath = dv.current().file.path;

// Приводим счётчики к числам с валидацией
let L = Math.max(0, Number(dv.current().light_cnt) || 0);
let M = Math.max(0, Number(dv.current().moral_cnt) || 0);
let T = Math.max(0, Number(dv.current().tragic_cnt) || 0);
let C = Math.max(0, Number(dv.current().chaos_cnt) || 0);

// Конфигурация путей
const paths = [
  { key: 'light_cnt',  label: '🕯️ К свету',      desc: 'Путь света и надежды',     cls: 'light',  emoji: '🕯️' },
  { key: 'moral_cnt',  label: '⚖️ Нейтралитет',  desc: 'Путь баланса и мудрости',  cls: 'moral',  emoji: '⚖️' },
  { key: 'tragic_cnt', label: '💀 К тьме',       desc: 'Путь жертвы и скорби',     cls: 'tragic', emoji: '💀' },
  { key: 'chaos_cnt',  label: '☠️ За хаос',      desc: 'Путь разрушения и бунта',  cls: 'chaos',  emoji: '☠️' }
];

// Функция создания кнопки изменения
function createActionBtn(label, prop, delta, cls, desc) {
  const btn = this.container.createEl('button', { 
    text: label, 
    cls: `btn btn-${cls}`,
    attr: { title: desc }
  });
  btn.onclick = async () => {
    const current = Math.max(0, Number(dv.current()[prop]) || 0);
    const newValue = Math.max(0, current + delta);
    await update(prop, String(newValue), filePath);
    
    // Анимация обновления
    btn.style.transform = 'scale(0.95)';
    setTimeout(() => { btn.style.transform = 'scale(1)'; }, 100);
    
    // Перерендер через небольшую задержку
    setTimeout(() => {
      this.container.empty();
      dv.current().file.renderer.render();
    }, 150);
  };
  return btn;
}

// Функция сброса
function createResetBtn() {
  const btn = this.container.createEl('button', { 
    text: '🔄 Новая история', 
    cls: 'btn btn-reset',
    attr: { title: 'Сбросить все счётчики и начать заново' }
  });
  btn.onclick = async () => {
    if (confirm('Вы уверены, что хотите начать новую историю?')) {
      for (const path of paths) {
        await update(path.key, '0', filePath);
      }
      this.container.empty();
      dv.current().file.renderer.render();
    }
  };
  return btn;
}

// === ИНТЕРФЕЙС УПРАВЛЕНИЯ ===
dv.header(2, '🎭 Предфинальные решения');
dv.paragraph('*Каждое решение формирует судьбу мира. Выберите путь героев:*');

// Создаём контейнер для кнопок
const controlPanel = this.container.createEl('div', { cls: 'control-panel' });

// Основные кнопки выбора
const mainActions = controlPanel.createEl('div', { cls: 'main-actions' });
paths.forEach(path => {
  const btnContainer = mainActions.createEl('div', { cls: 'btn-container' });
  const btn = createActionBtn.call(this, path.label, path.key, 1, path.cls, path.desc);
  btnContainer.appendChild(btn);
});

// Дополнительные действия
const extraActions = controlPanel.createEl('div', { cls: 'extra-actions' });
extraActions.appendChild(createResetBtn.call(this));

// Обновляем значения после возможных изменений
L = Math.max(0, Number(dv.current().light_cnt) || 0);
M = Math.max(0, Number(dv.current().moral_cnt) || 0);
T = Math.max(0, Number(dv.current().tragic_cnt) || 0);
C = Math.max(0, Number(dv.current().chaos_cnt) || 0);

const total = L + M + T + C;

// === СТАТИСТИКА ===
if (total > 0) {
  dv.header(3, '📊 Баланс сил');
  
  // Красивая таблица с процентами
  const statsTable = [
    ['Путь', 'Голосов', 'Влияние', 'Прогресс'],
    ...paths.map(path => {
      const value = Number(dv.current()[path.key]) || 0;
      const percentage = total > 0 ? Math.round((value / total) * 100) : 0;
      const bar = '█'.repeat(Math.floor(percentage / 5)) + '░'.repeat(20 - Math.floor(percentage / 5));
      return [
        `${path.emoji} ${path.label.split(' ').slice(1).join(' ')}`,
        value,
        `${percentage}%`,
        bar
      ];
    })
  ];
  
  dv.table(['Путь', 'Голосов', 'Влияние', 'Прогресс'], statsTable.slice(1));
  
  // Динамические уведомления о выборах
  dv.header(4, '🔮 Активные решения');
  const decisions = [
    { count: L, text: '🕯️ **Светлый путь**: Герои выбирают спасение и надежду', type: 'success' },
    { count: M, text: '⚖️ **Путь баланса**: Герои ищут компромисс и мудрость', type: 'info' },
    { count: T, text: '💀 **Тёмный путь**: Герои готовы на жертвы ради цели', type: 'warning' },
    { count: C, text: '☠️ **Путь хаоса**: Герои отвергают все правила', type: 'danger' }
  ];
  
  decisions.filter(d => d.count > 0).forEach(decision => {
    dv.paragraph(`> [!${decision.type}] ${decision.text} *(${decision.count} раз)*`);
  });
}

// === ВИЗУАЛИЗАЦИЯ РОМБА ===
if (total > 0) {
  dv.header(3, '💠 Карта судьбы');
  dv.paragraph('*Положение точки показывает текущий баланс сил в мире:*');
  
  // Улучшенная SVG диаграмма
  const size = 200;
  const center = size / 2;
  const radius = size * 0.4;
  
  // Вычисляем позицию на основе весов
  const lightWeight = L / total;
  const moralWeight = M / total;
  const tragicWeight = T / total;
  const chaosWeight = C / total;
  
  // Координаты (Light вверх, Moral вправо, Tragic вниз, Chaos влево)
  const x = center + (moralWeight - chaosWeight) * radius;
  const y = center + (tragicWeight - lightWeight) * radius;
  
  // Определяем доминирующую силу для цвета точки
  const maxValue = Math.max(L, M, T, C);
  let dominantColor = '#666';
  if (L === maxValue) dominantColor = '#4CAF50';
  else if (M === maxValue) dominantColor = '#FF9800';
  else if (T === maxValue) dominantColor = '#F44336';
  else if (C === maxValue) dominantColor = '#9C27B0';
  
  const svg = `
  <div class="fate-compass">
    <svg width="${size}" height="${size}" viewBox="0 0 ${size} ${size}">
      <!-- Фон и сетка -->
      <defs>
        <radialGradient id="bg" cx="50%" cy="50%" r="50%">
          <stop offset="0%" style="stop-color:#f8f9fa;stop-opacity:1" />
          <stop offset="100%" style="stop-color:#e9ecef;stop-opacity:1" />
        </radialGradient>
        <filter id="glow">
          <feGaussianBlur stdDeviation="2" result="coloredBlur"/>
          <feMerge> 
            <feMergeNode in="coloredBlur"/>
            <feMergeNode in="SourceGraphic"/>
          </feMerge>
        </filter>
      </defs>
      
      <!-- Основной ромб -->
      <polygon points="${center},20 ${size-20},${center} ${center},${size-20} 20,${center}" 
               fill="url(#bg)" stroke="#666" stroke-width="2" stroke-dasharray="5,3"/>
      
      <!-- Центральные линии -->
      <line x1="${center}" y1="20" x2="${center}" y2="${size-20}" stroke="#ddd" stroke-width="1"/>
      <line x1="20" y1="${center}" x2="${size-20}" y2="${center}" stroke="#ddd" stroke-width="1"/>
      
      <!-- Метки направлений -->
      <g font-family="monospace" font-size="14" text-anchor="middle" fill="#333">
        <text x="${center}" y="15" font-size="16">🕯️</text>
        <text x="${center}" y="35" font-size="8">СВЕТ</text>
        
        <text x="${size-10}" y="${center-5}" font-size="16">⚖️</text>
        <text x="${size-15}" y="${center+10}" font-size="8">МОРАЛЬ</text>
        
        <text x="${center}" y="${size-5}" font-size="16">💀</text>
        <text x="${center}" y="${size-15}" font-size="8">ТЬМА</text>
        
        <text x="15" y="${center-5}" font-size="16">☠️</text>
        <text x="15" y="${center+10}" font-size="8">ХАОС</text>
      </g>
      
      <!-- Текущая позиция -->
      <circle cx="${x}" cy="${y}" r="8" fill="${dominantColor}" stroke="#fff" stroke-width="2" filter="url(#glow)"/>
      <circle cx="${x}" cy="${y}" r="12" fill="none" stroke="${dominantColor}" stroke-width="1" opacity="0.5"/>
      
      <!-- Индикатор силы -->
      <text x="${x}" y="${y+25}" text-anchor="middle" font-size="10" fill="#333">
        ${total} решений
      </text>
    </svg>
  </div>
  `;
  
  this.container.createEl('div', { cls: 'fate-visualization' }).innerHTML = svg;
}

// === ПРЕДСКАЗАНИЕ ФИНАЛА ===
if (total >= 3) {
  dv.header(3, '🔮 Предсказание судьбы');
  
  const maxCount = Math.max(L, M, T, C);
  let prediction = '';
  
  if (L === maxCount && L > total * 0.4) {
    prediction = '> [!success] **Светлое будущее маячит на горизонте...** Герои найдут путь к спасению, хотя цена может быть высока.';
  } else if (M === maxCount && M > total * 0.4) {
    prediction = '> [!info] **Баланс ведёт к компромиссу...** Не всё будет потеряно, но и не всё будет спасено.';
  } else if (T === maxCount && T > total * 0.4) {
    prediction = '> [!warning] **Тени сгущаются...** Великие жертвы неизбежны, но из пепла может родиться новая надежда.';
  } else if (C === maxCount && C > total * 0.4) {
    prediction = '> [!danger] **Хаос набирает силу...** Старый мир рушится, но что поднимется из руин — неизвестно.';
  } else {
    prediction = '> [!question] **Судьба в равновесии...** Силы уравновешены, финал может стать неожиданным для всех.';
  }
  
  dv.paragraph(prediction);
}
```

---

## 🎯 Финальные развязки

*Выберите развязку, которая лучше всего отражает путь ваших героев:*

```button
name 🕯️ Светлая победа
type append text
action
---
> [!success] ## 🕯️ **СВЕТЛАЯ ПОБЕДА**
> 
> *Тьма рассеялась, как утренний туман перед восходящим солнцем.*
> 
> Последний герой поднимает дрожащую руку, и древняя магия отвечает на его призыв. Проклятие разрушено, деревня спасена, но память о произошедшем навсегда изменит тех, кто выжил. Один из героев теряет воспоминания о тёмных временах — возможно, это и к лучшему.
> 
> **Эпилог**: Через год на месте разрушенной часовни выросли удивительные цветы, которые никогда не вянут.

color green
```

```button
name ⚖️ Моральная развязка
type append text
action
---
> [!info] ## ⚖️ **ПУТЬ БАЛАНСА**
> 
> *Древние силы договорились. Цена определена.*
> 
> Сделка заключена между мирами живых и мёртвых. Герои получают силу, необходимую для защиты мира, но навсегда теряют доступ к высшим кругам магии. Они становятся стражами границы — не полностью живые, но и не мёртвые.
> 
> **Эпилог**: Героев больше не узнают в родных местах, но легенды о загадочных защитниках передаются из поколения в поколение.

color yellow
```

```button
name 💀 Трагический финал
type append text
action
---
> [!danger] ## 💀 **ТРАГЕДИЯ ПОСЛЕДНЕГО**
> 
> *Один остался среди пепла и воспоминаний.*
> 
> Тишина. Только ветер развевает пепел там, где были друзья. Последний герой смотрит на руины и понимает — победа далась слишком дорого. Все погибли, чтобы мир мог жить дальше. Слёзы на щеках превращаются в лёд от прикосновения вечности.
> 
> **Эпилог**: Спустя века, путешественники находят статую плачущего героя. Говорят, иногда она шевелится...

color red
```

```button
name ☠️ Хаотичная победа
type append text
action
---
> [!warning] ## ☠️ **ТОРЖЕСТВО ХАОСА**
> 
> *Силы пробуждены. Никто их больше не контролирует.*
> 
> Ритуал завершён, но никто не удержал высвобожденную энергию. Реальность искажается в радиусе многих миль. Законы природы перестают работать, время течёт вспять, а мёртвые танцуют с живыми. Герои получили то, что хотели, но мир изменился навсегда.
> 
> **Эпилог**: Новый мир рождается в хаосе. Что в нём будет нормой — покажет время.

color purple
```

---

<button type="link" target="Adventure_001.md" class="btn btn-secondary">← Назад к началу приключения</button>

